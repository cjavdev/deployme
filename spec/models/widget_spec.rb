require 'rails_helper'

RSpec.describe Widget, type: :model do
  xit "fails intentionally" do
    expect(true).to be(false)
  end

  it "passes intentionally" do
    expect(true).to be(true)
  end
end
